# Project Tracker

A Streamlit page where I track small tasks per project.

- **Input:** project name + description + tasks
- **Output:** a progress bar per project, and which tasks are done
- **Storage:** a local `projects.json` file
- **V1 does not:** reorder tasks by dragging

## Run it

pip install -r requirements.txt
streamlit run app.py
